pipeline {
    agent any

    stages {

        stage('runpy') {
            steps{
                sh '''
                python3 -m venv env
                . env/bin/activate
                pip install pytest
                python test_file.py
                '''
            }
        }

    }
}